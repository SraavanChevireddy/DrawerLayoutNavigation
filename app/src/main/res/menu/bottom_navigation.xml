<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:checked="true"
        android:id="@+id/offlineFragment"
        android:iconTint="@android:color/holo_orange_light"
        android:icon="@drawable/ic_home"
        android:title="offline dictations" />

    <item
        android:checked="true"
        android:iconTint="@android:color/holo_orange_light"
        android:id="@+id/homeFragment"
        android:icon="@drawable/ic_person"
        android:title="home bottom" />
    <item
        android:checked="true"
        android:id="@+id/case_search"
        android:icon="@drawable/ic_person"
        android:title="case search" />
    <item
        android:checked="true"
        android:id="@+id/schdules"
        android:icon="@drawable/ic_person"
        android:title="schedules" />
    <item
        android:checked="true"
        android:id="@+id/more"
        android:icon="@drawable/ic_person"
        android:title="more" />

</menu>